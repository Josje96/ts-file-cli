- [x] enable searching a path (`/` filters current dir; `f` searches the whole subtree)
- [ ] allow user to change for this application IE run as a different user
- [x] some sort of tree search (faster than file explorer) (`f` mode, skips node_modules/.git)
- [x] numbered folders/files for keyboard nav (press 1-9 to jump)

## Ideas for later
- delete / rename / copy from within the explorer (needs confirm-before-destroy UX)
- config file for keybindings
- preview pane for text files